# API Contracts

This directory contains canonical API contracts used by current automated tests.

- Update these contracts when the supported API changes.
- OpenSpec archive directories retain historical snapshots only.
- Tests must reference this stable directory rather than dated archive paths.
