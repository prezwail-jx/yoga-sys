# 功能清单（基于当前前端页面与后端能力）

> 最后更新：2026-07-30
> 状态说明：**已完成**＝真实前后端与数据库闭环；**部分完成**＝已有基础能力但缺少完整业务页面；**Mock**＝仅演示数据；**待建设**＝PRD 已提出但当前尚未实现。

## 1. 当前交付概览

| 模块 | 页面 | 状态 | 说明 |
|---|---|---|---|
| 登录与权限 | `/login`、`/forbidden` | 已完成 | 账号密码登录、HttpOnly Cookie、管理员/教练/会员角色识别与路由守卫 |
| 会员管理 | `/members` | 已完成 | 真实会员 CRUD、搜索、状态管理、分页与软删除 |
| 卡项管理 | `/cards` | 已完成 | 真实卡项模板 CRUD、规则校验与启停 |
| 卡项办理 | `/transactions` | 已完成 | 购卡、续费、补卡、退款、延期、冻结和解冻 |
| 会员业务时间线 | `/members/:memberId/timeline` | 已完成 | 交易、核销、审计混合时间线和核销链路抽屉 |
| 团课预约与核销 | `/schedule`、`/sessions/:sessionId`、`/my-bookings` | 已完成 | 管理员排课和运营、教练签到、会员预约与取消均接入真实服务 |
| 运营看板 | `/` | Mock | 当前仍是项目状态骨架，不是真实经营指标 |
| 团课课表 | `/schedule` | 已完成 | 真实周课表、课次编辑、复制上周及角色化操作入口 |
| 私教预约 | `/private-training` | 已完成 | 真实教练空闲时段、会员预约、确认/拒绝、待确认取消、签到和课时记录 |
| 统计报表 | `/reports` | 已完成 | 真实经营汇总、趋势、明细下钻和 Excel 导出 |

## 2. 全局框架与基础能力

### 2.1 已完成

- Nuxt 3 后台布局：左侧导航、顶部账号信息、内容区域。
- 菜单包含运营看板、会员管理、卡项管理、卡项办理、团课课表、私教预约和统计报表。
- Mock 模块在导航中明确显示 `Mock` 标识；私教预约和统计报表已移除 Mock 标识。
- FastAPI + PostgreSQL 真实后端，Alembic 当前迁移版本为 `0007_private_training_reporting`。
- Nuxt BFF 代理后端请求，登录态存放于 HttpOnly Cookie。
- 统一 API 客户端支持 GET、POST、PATCH、PUT、DELETE、查询参数和 `Idempotency-Key`。
- 统一异步状态组件覆盖加载中、空数据、请求失败和重试。
- 请求 Trace ID、结构化日志、OpenTelemetry Span 和审计日志。
- 独立测试数据库 `yoga_sys_test`，避免自动化测试影响开发数据。

### 2.2 权限模型

- 管理员：会员、卡项、交易、生命周期和业务记录管理。
- 教练：可以登录，管理本人课表和私教时段/预约/签到，但不能修改会员、卡项或财务类数据。
- 会员：可查看真实周课表和私教时段，预约本人、查看和取消本人待确认预约；资源身份取自 JWT。
- 前端路由守卫用于页面体验，最终权限以后端 JWT、角色和资源归属校验为准。
- 越权和被拒绝的关键请求写入审计日志。

## 3. 登录与账号（已完成）

- 支持管理员、教练和会员账号密码登录。
- 管理员可从会员与教练资料列表开通资源绑定账号。
- 登录成功后读取当前用户、角色及关联会员或教练档案 ID。
- 未登录访问受保护页面时进入登录流程。
- 无权限访问时进入 `/forbidden`。
- 支持退出登录并清除 Cookie。
- 初始账号从后端 `.env` 读取；生产环境必须替换默认密码和 JWT 密钥。

## 4. 会员管理（已完成）

### 4.1 列表与查询

- 展示姓名、手机号、状态、入会日期和操作。
- 支持按姓名或手机号模糊搜索。
- 支持按正常、暂停、到期、禁用状态筛选。
- 每页 20 条，支持上一页和下一页。
- 列表具备加载、空数据、错误和重试状态。

### 4.2 新增与编辑

- 字段：姓名、手机号、入会日期、性别、生日、紧急联系人、备注。
- 新增时校验必填字段、手机号和日期。
- 编辑时手机号和入会日期不可修改。
- 支持正常、暂停、禁用等状态转换。

### 4.3 归档与记录

- 归档采用软删除，历史交易、卡项、核销和审计记录继续保留。
- 每位会员可进入业务时间线。
- 每位会员可绑定多张会员卡。

## 5. 卡项模板管理（已完成）

- 卡项类型：期限卡、次数卡、私教卡、体验卡。
- 字段：名称、售价、成本价、总次数、有效期天数、开卡方式、适用课程、缺勤扣次、取消返还。
- 开卡方式：购卡即开、首次约课开卡。
- 适用范围：团课、私教、指定课程。
- 指定课程模式支持填写课程 ID 列表；创建和更新时校验 UUID 格式及课程存在性，重复 ID 自动去重。
- 表单根据卡类型动态显示次数或有效期字段并执行条件校验。
- 支持新建、编辑、启用和停用。
- 已购卡保存模板规则快照，后续修改模板不会反向改变历史权益。

## 6. 卡项办理与生命周期（已完成）

### 6.1 办理操作

- 管理员选择会员和启用中的卡项模板办理购卡。
- 支持续费、补卡、整笔退款和人工延期。
- 操作成功后展示交易号并刷新会员卡列表。
- 关键写操作使用幂等键，重复提交不会重复生效。

### 6.2 会员卡信息

- 展示卡项名称、卡类型、状态、剩余次数、已用次数、有效期、开卡日和到期日。
- 展示冻结区间、冻结原因、累计冻结天数和即将到期标识。
- 首次约课开卡在购买后保持待激活，首次有效预约时计算开卡日和到期日。
- 到期或次数耗尽时阻止继续使用。

### 6.3 冻结与解冻

- 支持填写冻结截止日期和冻结原因。
- 支持提前解冻并填写可选原因。
- 解冻后按自然冻结天数顺延有效期。
- 支持访问时自动校准冻结和过期状态。

## 7. 核销与业务时间线

### 7.1 团课预约与核销后端（已完成）

- 支持课程、教室、教练资料和真实课次，提供周课表、发布、暂停、恢复、取消、完成和复制周能力。
- 支持管理员代约和会员自助预约，校验会员、卡项、容量、时间冲突和预约窗口。
- 支持 `reserve_hold -> checkin_commit`：预约预扣后签到实扣。
- 支持 `reserve_hold -> cancel_refund`：预约预扣后取消返还。
- 支持结课批量 `absence_commit`，馆方取消强制返还预扣权益。
- 多张可用卡按 FEFO 规则选择：优先较早到期，再优先较早开卡。
- 行锁、唯一约束和幂等键共同防止超卖、重复预约和重复终态。
- 管理员课次运营台支持代约、取消、签到、整课取消和结课缺勤处理。
- 会员周课表和“我的预约”支持自助预约、取消及历史状态查看。
- 教练课表只显示本人课次，任课教练可进入运营台查看名单并签到。

### 7.2 会员业务时间线（已完成）

- 按会员查询交易、核销和审计事件。
- 支持类别、动作、开始日期和结束日期筛选。
- 支持分页、加载、空数据、错误和重试状态。
- 展示卡项名称和类型、金额、次数变化、有效期变化、原因、操作人和结果。
- 通过业务引用打开核销链路抽屉，查看预扣、实扣或返还的事件关系。
- 会员只能读取本人允许查看的记录，敏感审计与操作人信息按角色隐藏。

## 8. 当前 Mock 页面

### 8.1 运营看板（`/`）

- 当前只展示前端、后端和项目阶段状态。
- 尚未接入总会员、活跃会员、即将到期、收入和出勤指标。
- 页面中的 `Skeleton` 和“后续补业务页面”属于临时说明。

私教预约和统计报表已从 Mock 页面迁移为真实业务页面。

## 9. 真实业务接口

- 认证：登录、查询当前用户。
- 会员：新增、列表、详情、编辑、归档。
- 卡项模板：新增、列表、详情、编辑和启停。
- 会员卡：按会员查询、冻结和解冻。
- 交易：购卡、续费、补卡、退款和延期。
- 团课资料：课程、教室和教练资料 CRUD 及启停。
- 团课排课：课次 CRUD、周课表、生命周期和复制周。
- 账号绑定：管理员为会员和教练创建资源绑定账号。
- 团课预约：管理员代约、会员自助预约、取消、签到、课次取消和结课。
- 私教预约：时段发布、周批量生成、会员请求、确认、拒绝、待确认取消、签到和课时记录。
- 统计报表：汇总、趋势、明细下钻和 180 天内同步 Excel 导出。
- 核销：预约预扣、签到实扣、取消返还和缺勤实扣。
- 时间线：按会员、类别、动作和日期范围查询。
- 所有关键写接口具备幂等、事务、并发保护、Trace ID 和审计能力。

## 10. 数据库与性能优化

- PostgreSQL 保存会员、卡项模板、会员卡、交易、团课资料、课次、预约、私教时段、私教预约、私教课时记录、核销、幂等记录、账号和审计日志。
- 软删除不破坏历史业务链路。
- 启用 `pg_trgm` 加速会员姓名和手机号模糊搜索。
- 已增加有效会员、卡项模板、FEFO 卡选择和幂等过期记录索引。
- 已增加核销次数变化、幂等状态码和幂等过期时间约束。
- 已提供独立 `yoga_sys_perf` 数据库上的 smoke/target 性能入口；target 覆盖 20,000 会员、5,000 课次和 100,000 预约。
- 计划目标为关键写接口 p95 小于 300ms、50 条时间线查询 p95 小于 500ms；p95 为人工验收记录项，不作为 CI 硬门禁。

## 11. 后续建议功能

### P1：运营与报表增强

1. 将首页替换为真实运营看板。
2. 增加更多经营维度、对账报表和异步大批量导出。
3. 全局审计日志查询页。

### P3：通知与平台能力

1. 约课成功、课前提醒、取消/调课、私教确认和卡项到期通知。
2. 微信服务通知，短信作为可选通道。
3. 会员手机号验证码登录和完整会员端页面。
4. 自动备份、恢复演练、数据保留和归档策略。
5. 专项性能测试：关键写接口、时间线查询、1–2 万会员容量基线。

## 12. 当前验收状态

- 会员卡核心基线 Phase 1–6、T001–T063 已完成。
- 团课变更的数据模型、后端、账号绑定、真实前端页面及并发验证已完成。
- 管理员、教练和会员采用角色化导航与路由守卫，团课前端流程已有单元和 E2E 覆盖。
- 私教闭环和真实报表已完成真实前后端与数据库接入。
- 数据库迁移：`0007_private_training_reporting (head)`。
- 后端新增私教与报表单元测试；全量 contract 当前受缺失历史 OpenAPI fixture 阻塞。
- 前端新增私教/报表单元测试和 E2E 场景；最终全量构建结果见验收报告。
- 性能 smoke/target 尚未形成 p95 实测记录；本机数据库用户无权创建专用 `yoga_sys_perf` 数据库。
