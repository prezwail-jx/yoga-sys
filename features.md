# 功能清单（基于当前前端页面框架）

## 1. 全局框架与导航
- 后台管理布局：左侧导航 + 右侧内容区。
- 顶部系统信息展示：系统标题与当前运行说明（Mock API 模式）。
- 菜单路由已配置：
  - 运营看板（`/`）
  - 会员管理（`/members`）
  - 卡项管理（`/cards`）
  - 团课课表（`/schedule`）
  - 私教预约（`/private-training`）
  - 统计报表（`/reports`）

## 2. 运营看板（`/`）
- 系统元信息展示：系统名称、日期。
- 核心指标卡展示：
  - 总会员数
  - 活跃会员
  - 即将到期会员
- 经营概览指标展示：
  - 售卡收入
  - 续费收入
  - 退款金额
  - 团课出勤率（百分比）
  - 满课率（百分比）

## 3. 会员管理（`/members`）
- 会员列表展示：姓名、手机号、状态、入会日期。
- 会员搜索：支持按姓名或手机号关键字查询（前端触发刷新）。
- 会员详情查看：点击“查看卡项”后加载指定会员详情。
- 会员卡信息展示：卡项名称、卡类型、剩余次数、剩余天数、到期时间。

## 4. 卡项管理（`/cards`）
- 卡项列表展示：
  - 卡项名称
  - 类型
  - 售价
  - 总次数
  - 有效期（天）
  - 开卡方式
  - 适用课程

## 5. 团课课表（`/schedule`）
- 团课排班列表展示：
  - 星期（数字转中文周几）
  - 时间段
  - 课程名称
  - 教练
  - 教室
  - 已约/容量
  - 状态

## 6. 私教预约（`/private-training`）
- 私教空闲时段列表：教练、开始/结束、时长、预约状态。
- 预约记录列表：会员、课程、预约类型、预约时间、签到状态、扣次状态。

## 7. 统计报表（`/reports`）
- 汇总型报表卡片展示：
  - 总会员数
  - 活跃会员
  - 即将到期会员
  - 售卡收入
  - 续费收入
  - 退款金额
  - 团课出勤率
  - 满课率
- 页面内明确当前数据来源为 Mock，并预留后续替换真实聚合接口与导出能力的提示。

## 8. 前端数据访问能力（已封装）
- 统一 API 客户端（`/api` 前缀）支持：
  - `GET`/`POST`/`PATCH`/`PUT`/`DELETE`
  - query 参数拼接
  - 可选幂等键 `Idempotency-Key`
- 业务 API 已封装方法：
  - `getMeta`
  - `getMembers`
  - `getMember`
  - `getCards`
  - `getSchedule`
  - `getPrivateSlots`
  - `getBookings`
  - `getReportSummary`

## 9. Mock 服务能力（前端内置）
- 已提供页面所需 mock 接口：
  - `/api/meta`
  - `/api/members`
  - `/api/members/:id`
  - `/api/cards`
  - `/api/schedule`
  - `/api/private-slots`
  - `/api/bookings`
  - `/api/reports/summary`
- 已实现会员关键字过滤与会员详情 404 处理逻辑。

## 10. 当前边界（尚未在页面实现）
- 暂无新增/编辑/删除类表单操作（当前以展示和查询为主）。
- 暂无登录鉴权、角色权限控制的前端交互流程。
- 暂无分页、排序、导出、批量操作等增强管理能力。
- 暂无实时报表或图表可视化组件（当前为指标卡展示）。
