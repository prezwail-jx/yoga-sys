limit_req_zone $binary_remote_addr zone=wechat_auth:10m rate=10r/m;

server {
    listen 80;
    server_name yoga.tuitukj.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    http2 on;
    server_name yoga.tuitukj.com;

    ssl_certificate /etc/letsencrypt/live/yoga.tuitukj.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yoga.tuitukj.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    location /backend-trial/auth/wechat/ {
        limit_req zone=wechat_auth burst=10 nodelay;
        proxy_pass http://backend-trial:8000/auth/wechat/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /backend-trial/ {
        proxy_pass http://backend-trial:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /backend/auth/wechat/ {
        limit_req zone=wechat_auth burst=10 nodelay;
        proxy_pass http://backend-prod:8000/auth/wechat/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /backend/ {
        proxy_pass http://backend-prod:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location = /healthz {
        proxy_pass http://backend-prod:8000/healthz;
        access_log off;
    }

    location / {
        proxy_pass http://frontend-prod:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
