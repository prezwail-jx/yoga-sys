[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers --cov=app --cov-report=term-missing
markers =
    unit: fast isolated unit tests
    integration: tests with infra dependencies
    contract: API contract verification tests
