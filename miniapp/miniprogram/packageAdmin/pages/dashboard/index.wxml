<view class="page-shell"><view class="admin-header"><text class="eyebrow">运营概览</text><view class="title">经营数据</view><view class="muted">数据来自现有真实报表接口。</view></view><async-state wx:if="{{mode !== 'ready'}}" mode="{{mode}}" message="{{message}}" bindretry="load"/><view wx:else class="admin-grid"><view wx:for="{{metrics}}" wx:key="label" class="admin-metric"><text class="muted">{{item.label}}</text><text class="admin-metric__value">{{item.value}}</text></view></view></view>
